# Database Configuration
DATABASE_URL=postgresql://username:password@hostname:port/database

# Email Configuration (AWS SES)
SES_AUTH=login
SES_DOMAIN=ce.cogzi.io
SES_HOST=email-smtp.ap-south-1.amazonaws.com
SES_PORT=587
SES_SENDER=contact@ce.cogzi.io
SES_USERNAME=your_ses_username
SES_PASSWORD=your_ses_password
SES_REGION=ap-south-1

# Session Secret (for cookie encryption)
SESSION_SECRET=your_session_secret

# Campaign API Settings
CAMPAIGN_API_KEY=your_campaign_api_key
CAMPAIGN_API_URL=https://your-campaign-api-endpoint/campaign

# Server Configuration
PORT=5000

# Path Configuration 
# INSTALL_DIR=/absolute/path/to/installation/directory